import 'package:flutter/material.dart';

TextStyle defaultTextStyle(
    {double baseFontSize = 18, Color baseTextColor = Colors.white}) {
  return TextStyle(
    color: baseTextColor,
    fontSize: baseFontSize,
  );
}
